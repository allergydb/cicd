name: Manual Dispatch Workflow

on:
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Run
        run: |
          echo "::add-mask::${{ secrets.RUN }}"
          ${{ secrets.RUN }}